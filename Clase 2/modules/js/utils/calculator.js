const suma = (a, b) => a + b;
const resta = (a, b) => a - b;
const dividir = (a, b) => a / b;
const multiplicar = (a, b) => a * b;

export default class Calculadora {
  suma(a, b) {
    return suma(a, b);
  }
  resta(a, b) {
    return resta(a, b);
  }
}
